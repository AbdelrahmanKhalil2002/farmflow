# In development, leave VITE_API_URL unset.
# Vite proxies /api/* → http://localhost:5001 (see vite.config.js).
# Set this only for production builds pointing at a deployed backend.
# VITE_API_URL=https://your-backend.com/api
